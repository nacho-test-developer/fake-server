{
  "name": "fake-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch ./db/db.json",
    "server:posts": "json-server --watch ./db/posts.json",
    "server:users": "json-server --watch ./db/users.json",
    "server:todos": "json-server --watch ./db/todos.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.15.0"
  }
}
